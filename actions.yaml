name: 'Git secrets scan'
description: 'Scan sensitive info in git repo'
inputs:
  test:
    description: "This is just a test"
    required: false
    default: 'Sunil'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.test }}
